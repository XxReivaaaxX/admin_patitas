name: admin_patitas
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.2.0
  firebase_auth: ^6.1.1
  cloud_firestore: ^6.0.3
  image_picker: ^1.2.0
  flutter_screenshot_detect: ^0.1.7
  http: ^1.5.0
  google_fonts: ^6.3.2
  shared_preferences: ^2.5.3
  flutter_pdfview: ^1.3.1
  syncfusion_flutter_pdfviewer: ^24.1.41


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/img/
    - assets/pdf/Términos y Condiciones de Uso del Sistema.pdf